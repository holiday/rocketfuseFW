TODO:

- Extend and Refactor the Loader, it should be able to load Modules, Controllers, Views, CSS, JS and Images
- Complete version 1 of the Validator
- TEST....EVERYTHING!!! (PHPUnit)
- ACL should also handle redirect back if the feature is enabled
- Router should be better adapted to handle controller->view requests and display proper error pages
- Template only detects .php files, it should detect any extension




Validator:
- Must be able to validate Select fields (if they are set/not)
- Able to set dependencies between multiple fields eg. 2 fields that have upper/lower bounded values i.e. a price range
  where one cannot be less than the other or vice versa
- 